import { StyleSheet } from 'react-native';

export const animatedViewStyles = StyleSheet.create({
    animatedContainer: {
        alignItems: 'center',
        marginTop: 20,
    },
    animatedView: {
        width: 200,
        height: 50,
        backgroundColor: 'blue',
        justifyContent: 'center',
        alignItems: 'center',
        marginBottom: 10,
    },
    animatedText: {
        color: 'white',
        fontSize: 16,
    },
}); 