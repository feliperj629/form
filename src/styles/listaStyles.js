import { StyleSheet } from 'react-native';

export const listaStyles = StyleSheet.create({
    listaItem: {
        fontSize: 20,
        fontWeight: 'bold',
        color: 'green',
        marginBottom: 5,
        borderColor: 'gray',
        width: '100%',
        textAlign: 'center',
        marginTop: 5,
    },
}); 